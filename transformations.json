[
    ["Backdrop", "fork(recolor, color, backdrop)"],
    ["Delta", "fork(recolor, color, delta)"],
    ["Box", "fork(recolor, color, box)"],
    ["Outbox", "fork(recolor, color, outbox)"],
    ["OccupiedOnBox", "fork(recolor, color, fork(intersection, box, toindices))"],
    ["NotOccupiedOnBox", "fork(recolor, color, fork(difference, box, toindices))"],
    ["Trim", "fork(recolor, color, fork(difference, toindices, box))"],
    ["MirrorVertically", "vmirror"],
    ["MirrorHorizontally", "hmirror"],
    ["MirrorDiagonally", "dmirror"],
    ["MirrorCounterdiagonally", "cmirror"],
    ["ShiftDown", "rbind(shift, DOWN)"],
    ["ShiftUp", "rbind(shift, UP)"],
    ["ShiftLeft", "rbind(shift, LEFT)"],
    ["ShiftRight", "rbind(shift, RIGHT)"],
    ["ShiftDownRight", "rbind(shift, UNITY)"],
    ["ShiftUpLeft", "rbind(shift, NEG_UNITY)"],
    ["ShiftDownLeft", "rbind(shift, DOWN_LEFT)"],
    ["ShiftUpRight", "rbind(shift, UP_RIGHT)"],
    ["ToDiagonalLine", "fork(recolor, color, fork(connect, ulcorner, lrcorner))"],
    ["ToCounterdiagonalLine", "fork(recolor, color, fork(connect, llcorner, urcorner))"],
    ["Corners", "fork(recolor, color, corners)"],
    ["LargeOutline", "fork(recolor, color, fork(difference, lbind(mapply, compose(neighbors, last)), toindices))"],
    ["SmallOUtline", "fork(recolor, color, fork(difference, lbind(mapply, compose(dneighbors, last)), toindices))"],
    ["RemoveLooselyConnected", "fork(sfilter, identity, compose(lbind(compose, compose(positive, size)), compose(rbind(compose, compose(dneighbors, last)), compose(lbind(rbind, intersection), toindices))))"],
    ["FillHoles", "fork(combine, identity, fork(recolor, color, fork(sfilter, delta, chain(rbind(lbind(chain, compose(rbind(equality, FOUR), size)), dneighbors), lbind(lbind, intersection), toindices))))"],
    ["KeepLeftHalf", "fork(sfilter, identity, compose(rbind(compose, compose(last, last)), chain(lbind(lbind, greater), last, center)))"],
    ["RemoveLeftHalf", "fork(sfilter, identity, chain(rbind(compose, compose(last, last)), lbind(rbind, greater), chain(decrement, last, center)))"],
    ["KeepTopHalf", "fork(sfilter, identity, chain(rbind(compose, compose(first, last)), lbind(lbind, greater), compose(first, center)))"],
    ["RemoveTopHalf", "fork(sfilter, identity, compose(rbind(compose, compose(first, last)), compose(lbind(rbind, greater), chain(decrement, first, center))))"],
    ["KeepOccupiedCorners", "fork(recolor, color, fork(intersection, toindices, corners))"],
    ["MissingCorners", "fork(recolor, color, fork(difference, corners, toindices))"],
    ["TearHorizontally", "fork(combine, rbind(shift, LEFT), rbind(shift, RIGHT))"],
    ["TearVertically", "fork(combine, rbind(shift, UP), rbind(shift, DOWN))"],
    ["TearDiagonally", "fork(combine, rbind(shift, NEG_UNITY), rbind(shift, UNITY))"],
    ["TearCounterdiagonally", "fork(combine, rbind(shift, DOWN_LEFT), rbind(shift, UP_RIGHT))"],
    ["GravitateRightWithinBox", "fork(shift, fork(recolor, color, fork(difference, compose(asindices, fork(paint, compose(lbind(canvas, NEG_ONE), shape), normalize)), compose(rbind(ofcolor, NEG_ONE), compose(lbind(apply, rbind(order, identity)), fork(paint, compose(lbind(canvas, NEG_ONE), shape), normalize))))), ulcorner)"],
    ["GravitateLeftWithinBox", "fork(shift, fork(recolor, color, fork(difference, compose(asindices, fork(paint, compose(lbind(canvas, NEG_ONE), shape), normalize)), compose(rbind(ofcolor, NEG_ONE), compose(lbind(apply, rbind(order, compose(invert, identity))), fork(paint, compose(lbind(canvas, NEG_ONE), shape), normalize))))), ulcorner)"],
    ["RightLine", "fork(recolor, color, compose(rbind(shift, RIGHT), fork(connect, urcorner, lrcorner)))"],
    ["LeftLine", "fork(recolor, color, compose(rbind(shift, LEFT), fork(connect, ulcorner, llcorner)))"],
    ["TopLine", "fork(recolor, color, compose(rbind(shift, UP), fork(connect, ulcorner, urcorner)))"],
    ["BottomLine", "fork(recolor, color, compose(rbind(shift, DOWN), fork(connect, llcorner, lrcorner)))"],
    ["VerticalLines", "fork(recolor, color, fork(combine, compose(rbind(shift, RIGHT), fork(connect, urcorner, lrcorner)), compose(rbind(shift, LEFT), fork(connect, ulcorner, llcorner))))"],
    ["HorizontalLines", "fork(recolor, color, fork(combine, compose(rbind(shift, DOWN), fork(connect, llcorner, lrcorner)), compose(rbind(shift, UP), fork(connect, ulcorner, urcorner))))"],
    ["SymmetrizeVertically", "fork(combine, identity, vmirror)"],
    ["SymmetrizeHorizontally", "fork(combine, identity, hmirror)"],
    ["SymmetrizeDiagonally", "fork(combine, identity, dmirror)"],
    ["RotateOnce", "fork(shift, fork(recolor, color, fork(difference, compose(asindices, compose(rot90, fork(paint, compose(lbind(canvas, NEG_ONE), shape), normalize))), compose(rbind(ofcolor, NEG_ONE), compose(rot90, fork(paint, compose(lbind(canvas, NEG_ONE), shape), normalize))))), ulcorner)"],
    ["RotateTwice", "fork(shift, fork(recolor, color, fork(difference, compose(asindices, compose(rot180, fork(paint, compose(lbind(canvas, NEG_ONE), shape), normalize))), compose(rbind(ofcolor, NEG_ONE), compose(rot180, fork(paint, compose(lbind(canvas, NEG_ONE), shape), normalize))))), ulcorner)"],
    ["RotateThrice", "fork(shift, fork(recolor, color, fork(difference, compose(asindices, compose(rot270, fork(paint, compose(lbind(canvas, NEG_ONE), shape), normalize))), compose(rbind(ofcolor, NEG_ONE), compose(rot270, fork(paint, compose(lbind(canvas, NEG_ONE), shape), normalize))))), ulcorner)"]
]
